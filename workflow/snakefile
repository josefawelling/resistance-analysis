configfile: "config/config.yaml"

pepfile: config["pepfile"]

include: "rules/common.smk"
include: "rules/preprocessing.smk"
include: "rules/assembly.smk"

rule all:
    input:
        expand("results/{id}/flye", id=get_all_ids())
